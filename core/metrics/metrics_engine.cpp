// metrics_engine.cpp
// MetricsEngine is fully header-only (all sub-engines are .hpp templates).
// This file exists only so CMakeLists.txt has a .cpp to compile for the
// metrics/ translation unit — ensures the linker sees the module.
#include "metrics_engine.hpp"
